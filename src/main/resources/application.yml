spring:
  profiles:
    active: dev
  application:
    name: fms-refund

#分页pageHelper
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true

mybatis:
  #mysql 环境
  mapperLocations: classpath:mysql/com/ddky/refund/*.xml
logging:
  config: classpath:log4j2.xml