<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ddky.fms.refund.mapper.books.BiologyBookMapper">

    <sql id="column">
        bookId, bookName, shortName, publisher, theYear, coverImage, cipImage, gradeId
    </sql>

    <sql id="where">
        <if test="bookName != null">
            and bookName = #{bookName}
        </if>
        <if test="shortName != null">
            and shortName = #{shortName}
        </if>
        <if test="publisher != null">
            and publisher = #{publisher}
        </if>
        <if test="theYear != null">
            and theYear = #{theYear}
        </if>
        <if test="gradeId != null">
            and gradeId = #{gradeId}
        </if>
    </sql>

    <select id="list" resultType="com.ddky.fms.refund.model.books.chinese.entry.ChineseBook">
        select
            <include refid="column" />
        from bok_chinese_info
        <where>
            <include refid="where" />
        </where>
    </select>

    <select id="findById" resultType="com.ddky.fms.refund.model.books.chinese.entry.ChineseBook">
        select
            <include refid="column" />
        from bok_chinese_info
        <where>
            bookId = #{bookId}
        </where>
    </select>

    <insert id="insert" parameterType="com.ddky.fms.refund.model.books.biology.entry.ChineseBook">
        insert into bok_chinese_info (bookName, shortName, publisher, theYear, coverImage, cipImage, gradeId)
        values (#{bookName}, #{shortName}, #{publisher}, #{theYear}, #{coverImage}, #{cipImage}, #{gradeId})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        <foreach collection="list" index="index" item="item" separator=";">
            insert into bok_chinese_info (bookName, shortName, publisher, theYear, coverImage, cipImage, gradeId)
            values (#{item.bookName}, #{item.shortName}, #{item.publisher}, #{item.theYear}, #{item.coverImage}, #{item.cipImage}, #{item.gradeId})
        </foreach>
    </insert>

</mapper>