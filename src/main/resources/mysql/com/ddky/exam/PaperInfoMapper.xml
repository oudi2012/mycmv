<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ddky.fms.refund.mapper.exam.PaperInfoMapper">

    <sql id="column">
        id, subjectId, gradeId, title, creatorId, createTime, updateTime, examTime, examLong, expireLong
    </sql>

    <sql id="where">
        <if test="title != null">
            and title = #{title}
        </if>
        <if test="subjectId != null">
            and subjectId = #{subjectId}
        </if>
        <if test="gradeId != null">
            and gradeId = #{gradeId}
        </if>
    </sql>

    <select id="list" resultType="com.ddky.fms.refund.model.exam.entry.PaperInfo">
        select
            <include refid="column" />
        from exam_paper_info
        <where>
            <include refid="where" />
        </where>
    </select>

    <select id="findById" resultType="com.ddky.fms.refund.model.exam.entry.PaperInfo">
        select
            <include refid="column" />
        from exam_paper_info
        <where>
            id = #{id}
        </where>
    </select>

    <insert id="insert" parameterType="com.ddky.fms.refund.model.exam.entry.PaperInfo">
        insert into exam_paper_info (subjectId, gradeId, title, creatorId, examTime, examLong, expireLong)
        values (#{subjectId}, #{gradeId}, #{title}, #{creatorId}, #{examTime}, #{examLong}, #{expireLong})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        <foreach collection="list" index="index" item="item" separator=";">
            insert into exam_paper_info (subjectId, gradeId, title, creatorId, examTime, examLong, expireLong)
            values (#{item.subjectId}, #{item.gradeId}, #{item.title}, #{item.creatorId}, #{item.examTime}, #{item.examLong}, #{item.expireLong})
        </foreach>
    </insert>

</mapper>